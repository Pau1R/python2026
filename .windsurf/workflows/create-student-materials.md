---
description: Создание теоретического материала для нового урока
auto_execution_mode: 1
---

 **Обязательное требование.** Все шаги ниже выполняются строго по порядку, по одному шагу за раз, без пропуска шагов и без объединения нескольких шагов в один.

 ## 0. Роль воркфлоу. артефакты. границы ответственности

 Этот воркфлоу нужен, чтобы стабильно создавать качественный теоретический материал урока, а не просто генерировать текст.

 **Единственный артефакт.** создается ровно один файл теории `THEORY_FILE` в репозитории курса.

 **Запрещено в рамках воркфлоу.**
 - не создавать и не менять `homework.md`
 - не добавлять разделы про домашнее задание, практику, задачи
 - не изменять репозитории `python2026_tasks` и `students`

 **Цель качества.** студент понимает тему без сторонних источников. После чтения он может повторить примеры и объяснить, что делает код.

 **Правило стиля.** не использовать em dash. вместо этого использовать точку или запятую.

 **Правило читаемости.** после точки новое предложение начинается с заглавной буквы.

 **Правило символов.** допускаются только маркеры 🟢 🟡 🔴 в заголовках `###`. прочие эмодзи не использовать.

 ## 1. Входные данные урока

 1.1. Получить от пользователя:
 - номер урока `lesson_number_raw` как целое число (пример: `32`)
 - дату занятия `lesson_date_raw` в формате `ДД.ММ` (пример: `08.12`)
 - тему занятия `lesson_topic_raw` как строку на русском языке

 1.2. Если пользователь уже явно указал номер, дату и тему в запросе:
 - не просить подтверждение
 - сразу использовать эти значения как источник истины

 1.3. Нормализовать входные данные:
 - `lesson_number = int(lesson_number_raw)`
 - `lesson_date = lesson_date_raw`
 - `lesson_topic = lesson_topic_raw.strip()`

 1.4. Правило для файловой системы:
 - в `lesson_topic` не использовать обратные кавычки `
 - если тема содержит `*args`, `**kwargs` или другие фрагменты, которые хочется подсветить как код, подсветку делать только в тексте теории и в `lesson_title`. но не в имени папки, имени файла и пути `THEORY_FILE`

 ## 2. Контекст репозиториев и вычисляемые пути

 2.1. Зафиксировать корни репозиториев:
 - `PYTHON2026_ROOT` (репозиторий теории)
 - `PYTHON2026_TASKS_ROOT` (репозиторий задач)
 - `STUDENTS_ROOT` (репозиторий студентов)

 2.2. Примеры значений путей (если нужно свериться):
 - `PYTHON2026_ROOT = /Users/paul/Personal/Работа/Академия ТОП/Группы/Python2026/python2026`
 - `PYTHON2026_TASKS_ROOT = /Users/paul/Personal/Работа/Академия ТОП/Группы/Python2026/python2026_tasks`
 - `STUDENTS_ROOT = /Users/paul/Personal/Работа/Академия ТОП/Группы/Python2026/students`

 2.3. Вычислить пути:
 - `LESSONS_DIR = PYTHON2026_ROOT/lessons`
 - `TASKS_LESSONS_DIR = PYTHON2026_TASKS_ROOT/lessons`
 - `COURSE_FILE = PYTHON2026_ROOT/course.md`

 ## 3. Сверка нумерации урока

 3.1. В `LESSONS_DIR` найти каталоги, имя которых соответствует шаблону `lesson <число> (`.
 3.2. Извлечь `<число>` и вычислить `max_lesson_number`. если каталогов нет, считать `max_lesson_number = 0`.

 3.3. В `TASKS_LESSONS_DIR` сделать то же самое и вычислить `max_tasks_lesson_number`. если каталогов нет, считать `max_tasks_lesson_number = 0`.

 3.4. Вычислить ожидаемый следующий номер:
 - `expected_next_lesson_number = max(max_lesson_number, max_tasks_lesson_number) + 1`

 3.5. Если `lesson_number != expected_next_lesson_number`:
 - зафиксировать предупреждение о рассинхронизации
 - продолжить выполнение. использовать входной `lesson_number` как источник истины

 ## 4. Имя урока. заголовок. путь к файлу

 4.1. Сформировать строку для файловой системы:
 - `lesson_name = "lesson " + str(lesson_number) + " (" + lesson_date + "). " + lesson_topic`

 4.2. Сформировать заголовок урока для теории:
 - `lesson_title = "Урок " + str(lesson_number) + " (" + lesson_date + "). " + lesson_topic`

 4.3. Разрешить форматирование в `lesson_title`:
 - если в теме есть `*args` или `**kwargs`, обернуть их в `lesson_title` в обратные кавычки
 - при этом `lesson_name`, `THEORY_DIR`, `THEORY_FILE` должны оставаться без обратных кавычек

 4.4. Вычислить:
 - `THEORY_DIR = LESSONS_DIR + "/" + lesson_name`
 - `THEORY_FILE = THEORY_DIR + "/" + lesson_name + ".md"`

 ## 5. Контекст из `course.md` (если есть)

 5.1. Прочитать `COURSE_FILE`.
 5.2. Найти первую строку, содержащую `lesson_topic` (кейс не учитывать).

 5.3. Если совпадение найдено, зафиксировать:
 - `course_topic_line` (строка темы)
 - `course_section_title` (ближайший заголовок `## ...` выше строки темы)
 - `course_marker` (🟢 или 🟡 или 🔴), если указан
 - `course_priority` (число в скобках), если указано
 - `course_subtopics` (подпункты темы, которые идут сразу под строкой темы)

 5.4. Если совпадение не найдено:
 - считать `course_section_title`, `course_marker`, `course_priority`, `course_subtopics` пустыми
 - продолжить, опираясь на `lesson_topic`
 - зафиксировать действие на будущее. добавить тему урока в `course.md` отдельным изменением, но не в рамках этого воркфлоу

 ## 6. Референс по стилю из существующих уроков

 6.1. Найти один последний существующий файл теории в `LESSONS_DIR`.
 6.2. Прочитать его и зафиксировать стиль:
 - формат заголовков
 - формат ключевых понятий
 - формат объяснений под кодом

 6.3. Зафиксировать 3–5 конкретных правил, которые надо сохранить в новом файле.

 ## 7. План документа. карта покрытия. подтверждение

 7.1. Составить план теории:
 - `## Цель урока` (2–4 предложения)
 - `## Ключевые понятия` (черновик 6–12 пунктов)
 - 3–6 теоретических блоков `###` с маркером сложности 🟢 или 🟡 или 🔴
 - идея `## Объединяющий пример` (мини сюжет. что демонстрируем. какие результаты ожидаем)

 7.2. Сделать карту покрытия:
 - для каждого ключевого понятия указать, где оно объясняется и где используется в коде
 - если понятие не покрывается, удалить его или добавить блок, где оно будет использоваться

 7.3. Если `course_subtopics` не пуст:
 - отметить, какие подпункты покрыты явно
 - какие объединены
 - какие пропущены и почему (повтор, выход за уровень, не нужно для цели урока)

 7.4. Показать пользователю план и карту покрытия.

 7.5. Правило подтверждения:
 - если пользователь уже подтвердил план словами `ok`, `подтверждаю`, `подтверждено` или эквивалентом, считать план подтвержденным
 - если пользователь заранее попросил не запрашивать подтверждение плана, считать план подтвержденным
 - иначе запросить подтверждение и не переходить дальше без него

 ## 8. Генерация текста `THEORY_FILE` по утвержденному плану

 8.1. Собрать документ строго по шаблону:
 - `# <lesson_title>`
 - пустая строка
 - `## Цель урока`
 - `## Ключевые понятия`
 - теоретические блоки `### 🟢 ...`, `### 🟡 ...`, `### 🔴 ...`
 - `## Объединяющий пример`

 8.2. Правила содержания:
 - в каждом теоретическом блоке минимум один блок кода `python`
 - под каждым блоком кода есть объяснение списком:
   - `- Что приходит на вход.`
   - `- Что делает код шаг за шагом.`
   - `- Что возвращается или печатается.`
   - `- Частая ошибка.` или `- Подводный камень.`
 - минимум в двух местах в документе явно указать ожидаемый результат (строка или число)
 - если в объяснении указан конкретный вывод, он должен совпадать с реальным выводом `print`
 - не использовать недосказанность. не оставлять `...`, `TODO`, псевдокод вместо кода
 - код примеров самодостаточный. все переменные определены. функции определены до использования
 - если используются понятия вне темы, объяснить их коротко и строго в пределах примера
 - избегать конструкций, которые еще не проходили. если без них нельзя, добавить короткое пояснение, зачем они нужны здесь
 - каждый теоретический блок `### ...` заканчивается коротким резюме 1–2 предложения. резюме отвечает на вопрос, зачем это нужно и что студент должен уметь после блока
 
 8.3. Правила для частных случаев:
 - если в ключевых понятиях есть `global` или `nonlocal`, в тексте должен быть минимум один корректный пример с реальным использованием. иначе удалить эти термины из списка ключевых понятий
 - если тема включает `*args` или `**kwargs`, добавить пример с распаковкой при вызове `func(*some_list)` или `func(**some_dict)`
 - если в тексте используется `*` для keyword-only параметров (пример: `def f(*, x=1): ...`), это должно быть отдельным ключевым понятием и отдельным объясненным блоком. либо этот синтаксис не должен появляться в документе
 - в `## Объединяющий пример` запрещено вводить новый синтаксис или новые понятия, которых не было в теоретических блоках. если нужно, добавить отдельный блок выше

 ## 9. Самопроверка перед записью файла

 9.1. Проверить структуру:
 - есть `# <lesson_title>`
 - есть `## Цель урока`
 - есть `## Ключевые понятия`
 - есть минимум 3 теоретических блока `###` и в каждом есть маркер 🟢 или 🟡 или 🔴
 - есть `## Объединяющий пример`

 9.2. Проверить примеры:
 - в каждом теоретическом блоке минимум один блок кода `python`
 - после каждого блока кода есть список из 4 пунктов, начинающихся с нужных формулировок
 - в примерах нет недописанности и скрытых зависимостей
 - в `## Объединяющий пример` нет нового синтаксиса и новых понятий, которых не было в теоретических блоках
 - после каждого предложения нет систематического нарушения заглавных букв (точка. новая фраза с маленькой буквы)

 9.2.1. Проверить стиль и символы (это обязательные механические проверки):
 - в документе нет em dash `—`
 - в документе нет систематических мест, где после точки идет русская строчная буква. для поиска использовать regex `\.\s+[а-яё]` в режиме case-sensitive
 - в заголовках `###` после пробела используется только маркер 🟢 или 🟡 или 🔴. проверка. нет совпадений для regex `^###\s+[^🟢🟡🔴]` и есть совпадения для regex `^###\s+[🟢🟡🔴]`

 9.3. Проверить запреты:
 - в документе нет разделов и заголовков про `Домашнее задание`, `Практика`, `Задачи для самостоятельного решения`

 9.4. Оценить качество по рубрике (минимум 14 из 18):
 - **Структура и навигация (0–3).** структура соблюдена. заголовки понятные.
 - **Покрытие темы (0–3).** ключевые понятия реально объяснены и используются.
 - **Точность и самодостаточность (0–3).** определения корректны. код запускается как есть.
 - **Педагогика и пояснения (0–3).** понятны вход, шаги, выход. отмечены ошибки.
 - **Прогрессия сложности (0–3).** от простого к сложному.
 - **Язык и консистентность (0–3).** короткие предложения. ключевые слова Python в inline code. нет em dash.

 9.5. Если результат ниже порога, вернуться к шагу 8 и переработать слабые места.

 9.6. Проверить корректность путей:
 - `THEORY_FILE` и `THEORY_DIR` не содержат обратных кавычек

 9.7. Проверить соответствие контексту из `course.md` (если он есть):
 - если `course_subtopics` не пуст, убедиться, что ключевые смыслы отражены (допускается объединение и переформулирование)
 - если `course_marker` задан, подобрать глубину по маркеру (🟢 база. 🟡 база плюс практика. 🔴 обзорно плюс один рабочий пример)

 ## 10. Запись артефакта и отчет

 10.1. Если `THEORY_DIR` не существует, создать каталог рекурсивно.
 10.2. Если `THEORY_FILE` уже существует, запросить у пользователя подтверждение перезаписи.
 10.3. Записать итоговый текст в `THEORY_FILE` в кодировке UTF-8.
 10.4. Повторно прочитать `THEORY_FILE` и убедиться, что содержимое совпадает с сгенерированным текстом.

 10.5. Вернуть итоговый лог:
 - `lesson_number`
 - `lesson_name`
 - путь `THEORY_FILE`
 - предупреждение о рассинхронизации, если было
 - результат самопроверки (баллы и краткие замечания)