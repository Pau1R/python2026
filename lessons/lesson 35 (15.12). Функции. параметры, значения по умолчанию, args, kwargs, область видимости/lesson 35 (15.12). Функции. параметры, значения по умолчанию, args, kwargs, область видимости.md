<<<FILE_START>>>
# Урок 35 (15.12). Функции. Параметры, значения по умолчанию, `*args`, `**kwargs`, область видимости

## Цель урока
Функции позволяют выносить повторяющийся код в одно место и давать ему понятное имя. На уроке ты научишься объявлять функции, передавать аргументы разными способами и получать результат через `return`. Ты разберешься со значениями по умолчанию и типичной ловушкой изменяемых объектов. Ты поймешь, почему переменные иногда не видны внутри функции, и как работает область видимости.

## Ключевые понятия
- Функция `def` и вызов функции
- Параметр и аргумент
- Позиционные и именованные аргументы. Порядок передачи
- `return` и возвращаемое значение. `None`
- Оператор `in` для проверки принадлежности
- Метод списка `.append()`
- Встроенные функции `max()`, `min()`, `sum()`
- Значения по умолчанию
- Ловушка изменяемых значений по умолчанию (list, dict)
- `*args` и распаковка итерируемых (`*`) при вызове
- `**kwargs` и распаковка словарей (`**`) при вызове
- Область видимости. Локальная и глобальная
- `global` как явное изменение глобальной переменной
- `nonlocal` как изменение переменной из внешней функции
- Замыкание как идея. Функция внутри функции помнит внешние значения

### 🟢 Зачем нужны функции. Параметры, аргументы, return
Функция в Python объявляется через `def`. В скобках пишутся параметры. Это имена, которые будут доступны внутри функции. Когда ты вызываешь функцию, ты передаешь аргументы. Это конкретные значения.

`return` завершает выполнение функции и возвращает значение наружу. Если `return` не написать, функция вернет `None`.

```python
def area(width, height):
    result = width * height
    return result

s = area(3, 5)
print(s)
```
- Что приходит на вход.
  - `width=3`, `height=5`.
- Что делает код шаг за шагом.
  - Умножает `width` на `height`.
  - Сохраняет в `result`.
  - Возвращает `result`.
- Что возвращается или печатается.
  - Возвращается число `15`.
  - Печатается `15`.
- Частая ошибка.
  - Путать `print` и `return`. Если функция только печатает, ее результат нельзя использовать в вычислениях.

```python
def greet(name):
    print(f"Привет, {name}!")

value = greet("Анна")
print(value)
```
- Что приходит на вход.
  - `name="Анна"`.
- Что делает код шаг за шагом.
  - Функция печатает приветствие.
  - Явного `return` нет.
  - После вызова в `value` попадет то, что вернула функция.
- Что возвращается или печатается.
  - Печатается строка `Привет, Анна!`.
  - Затем печатается `None`.
- Подводный камень.
  - Новички ожидают, что `greet` вернет строку. Но она ее только печатает. Чтобы вернуть строку, нужен `return`.

В этом блоке важно научиться отличать входные аргументы, внутреннюю работу и результат функции. Это основа для более сложных параметров.
После этого блока выполните практическое задание.

Также пригодятся базовые операции, которые часто используются внутри функций, работающих со списками.

- Оператор `in` проверяет принадлежность элемента коллекции.

```python
planets = ["Земля", "Марс", "Венера"]
print("Марс" in planets)
```

- Метод списка `.append()` добавляет элемент в конец списка.

```python
items = []
items.append("молоко")
print(items)
```

- Встроенные функции `max()`, `min()`, `sum()` помогают находить максимум, минимум и сумму в коллекции чисел.

```python
numbers = [10, 5, 30]
print(max(numbers))
print(min(numbers))
print(sum(numbers))
```

В задачах часто встречается паттерн. Функция принимает список, проходит по нему циклом `for` и что-то вычисляет.

### 🟡 Позиционные и именованные аргументы. Порядок передачи
Аргументы можно передавать позиционно. Тогда значения сопоставляются параметрам по порядку. Аргументы можно передавать по имени. Тогда порядок не важен.

Правило порядка в вызове.
Сначала идут позиционные аргументы, потом именованные.

```python
def format_price(amount, currency):
    return f"{amount} {currency}"

print(format_price(150, "RUB"))
print(format_price(currency="RUB", amount=150))
```
- Что приходит на вход.
  - В первом вызове `amount=150`, `currency="RUB"` передаются позиционно.
  - Во втором вызове те же значения передаются именованно.
- Что делает код шаг за шагом.
  - Собирает строку из суммы и валюты.
  - Возвращает эту строку.
- Что возвращается или печатается.
  - Печатается `150 RUB`.
  - Печатается `150 RUB`.
- Частая ошибка.
  - Передать аргумент дважды. Например `format_price(150, amount=200)`.

```python
def delivery_cost(city, weight_kg, urgent):
    if urgent:
        return 500 + 30 * weight_kg
    return 300 + 20 * weight_kg

print(delivery_cost("Казань", 2, urgent=True))
```
- Что приходит на вход.
  - `city="Казань"`, `weight_kg=2` позиционно.
  - `urgent=True` именованно.
- Что делает код шаг за шагом.
  - Проверяет `urgent`.
  - Для срочной доставки считает `500 + 30 * weight_kg`.
- Что возвращается или печатается.
  - Возвращается `560`.
  - Печатается `560`.
- Подводный камень.
  - Ошибка синтаксиса, если написать так. `delivery_cost(city="Казань", 2, urgent=True)`.

В этом блоке ты должен уметь читать вызовы функций и видеть, какой параметр какое значение получит. Это особенно важно, когда параметров много.
После этого блока выполните практическое задание.

### 🟡 Значения по умолчанию. Ловушка с изменяемыми объектами
Значение по умолчанию задается прямо в объявлении функции. Оно используется, если аргумент не передали.

Важно помнить правило.
Значения по умолчанию вычисляются один раз, в момент объявления функции. Поэтому изменяемый объект, например список, будет общим между вызовами.

Плохой пример.

```python
def add_item_bad(item, items=[]):
    items.append(item)
    return items

print(add_item_bad("молоко"))
print(add_item_bad("хлеб"))
```
- Что приходит на вход.
  - В первом вызове `item="молоко"`, `items` не передан.
  - Во втором вызове `item="хлеб"`, `items` снова не передан.
- Что делает код шаг за шагом.
  - Использует один и тот же список `items` как значение по умолчанию.
  - Добавляет `item` через `append`.
  - Возвращает список.
- Что возвращается или печатается.
  - Печатается `['молоко']`.
  - Печатается `['молоко', 'хлеб']`.
- Частая ошибка.
  - Ожидать, что каждый вызов создаст новый пустой список. Но список один и он копится.

Правильный паттерн.
Использовать `None`, а список создавать внутри функции.
Этот подход часто называют паттерном изменяемых значений по умолчанию. Или `None` pattern.
Он основан на условии `if items is None:`. Это пример того, как `if` помогает безопасно управлять потоком выполнения.

```python
def add_item(item, items=None):
    if items is None:
        items = []
    items.append(item)
    return items

print(add_item("молоко"))
print(add_item("хлеб"))
```
- Что приходит на вход.
  - В обоих вызовах `items=None`.
- Что делает код шаг за шагом.
  - Проверяет `items is None`.
  - Создает новый список внутри функции.
  - Добавляет `item`.
  - Возвращает список.
- Что возвращается или печатается.
  - Печатается `['молоко']`.
  - Печатается `['хлеб']`.
- Подводный камень.
  - Перепутать проверку с `if not items:`. Такой вариант сломается, если пользователь передал пустой список намеренно.

В этом блоке цель одна. Научиться безопасно работать со значениями по умолчанию и узнавать баг с общей изменяемой коллекцией.
После этого блока выполните практическое задание.

### 🟡 `*args` и `**kwargs`. Переменное число аргументов и распаковка
Иногда функция должна принимать разное количество аргументов. Для этого используют `*args` и `**kwargs`.

- `*args` собирает лишние позиционные аргументы в кортеж.
- `**kwargs` собирает лишние именованные аргументы в словарь.
Кортеж это тип данных `tuple`. Его часто используют как неизменяемую последовательность. Функции также могут возвращать кортежи.

```python
def mean(*args):
    total = sum(args)
    return total / len(args)

print(mean(10, 20, 30))
```
- Что приходит на вход.
  - Позиционные аргументы `10, 20, 30`.
  - Внутри функции они попадают в `args=(10, 20, 30)`.
- Что делает код шаг за шагом.
  - Считает сумму `sum(args)`.
  - Делит на количество `len(args)`.
  - Возвращает среднее.
- Что возвращается или печатается.
  - Возвращается `20.0`.
  - Печатается `20.0`.
- Частая ошибка.
  - Вызвать `mean()` без аргументов. Тогда `len(args)` будет 0 и возникнет `ZeroDivisionError`.

Распаковка списка при вызове.
Это называют распаковкой итерируемого. Или iterable unpacking.

```python
def report(a, b, c):
    print(a, b, c)

values = [1, 2, 3]
report(*values)
```
- Что приходит на вход.
  - Список `values=[1, 2, 3]`.
  - При вызове `report(*values)` элементы распаковываются как `report(1, 2, 3)`.
- Что делает код шаг за шагом.
  - Распаковывает список в отдельные позиционные аргументы.
  - Печатает три значения.
- Что возвращается или печатается.
  - Печатается `1 2 3`.
- Подводный камень.
  - Количество элементов должно совпадать с количеством параметров. Иначе будет `TypeError`.

Пример для `**kwargs`.

```python
def build_user(**kwargs):
    name = kwargs.get("name", "Без имени")
    age = kwargs.get("age", 0)
    return {"name": name, "age": age, "extra": kwargs}

print(build_user(name="Игорь", age=20, city="Казань"))
```
- Что приходит на вход.
  - Именованные аргументы `name`, `age`, `city`.
  - Внутри функции они попадают в словарь `kwargs`.
- Что делает код шаг за шагом.
  - Достает обязательные поля через `get`.
  - Собирает итоговый словарь с основными полями и всем остальным.
- Что возвращается или печатается.
  - Печатается `{'name': 'Игорь', 'age': 20, 'extra': {'name': 'Игорь', 'age': 20, 'city': 'Казань'}}`.
- Частая ошибка.
  - Пытаться обращаться к `kwargs.name`. Это словарь, нужно `kwargs["name"]` или `kwargs.get("name")`.

Распаковка словаря при вызове.
Это называют распаковкой словаря. Или dictionary unpacking.

```python
def show_point(x, y):
    print(f"({x}, {y})")

point = {"x": 7, "y": 9}
show_point(**point)
```
- Что приходит на вход.
  - Словарь `point={"x": 7, "y": 9}`.
  - При вызове `show_point(**point)` пары ключ значение распаковываются как именованные аргументы.
- Что делает код шаг за шагом.
  - Передает `x=7` и `y=9` в функцию.
  - Печатает координаты.
- Что возвращается или печатается.
  - Печатается `(7, 9)`.
- Подводный камень.
  - Ключи в словаре должны совпадать с именами параметров. Иначе будет `TypeError`.

В этом блоке ты должен понять две вещи. Как собирать аргументы в `args` и `kwargs`, и как распаковывать список и словарь в момент вызова.
После этого блока выполните практическое задание.

### 🔴 Область видимости. Локальные и глобальные переменные. global
Переменные, созданные внутри функции, являются локальными. Снаружи их не видно. Переменные, созданные на уровне файла, являются глобальными.

Если внутри функции просто написать `x = ...`, это создаст локальную переменную. Если ты хотел изменить глобальную переменную, нужно явно сказать об этом через `global`.

```python
counter = 0

def hit():
    global counter
    counter += 1
    return counter

print(hit())
print(hit())
print(counter)
```
- Что приходит на вход.
  - Функция `hit` ничего не принимает.
- Что делает код шаг за шагом.
  - Объявляет, что `counter` внутри функции является глобальной переменной.
  - Увеличивает `counter` на 1.
  - Возвращает новое значение.
- Что возвращается или печатается.
  - Печатается `1`.
  - Печатается `2`.
  - Печатается `2`.
- Подводный камень.
  - Часто `global` превращает код в трудно тестируемый. Обычно лучше возвращать новое значение и присваивать его снаружи.

Еще одна полезная идея. Замыкание.
Функция, объявленная внутри другой функции, может использовать переменные внешней функции. При этом эти значения сохраняются.
Это и есть замыкание. Внутренняя функция замыкает внешнюю переменную. Или closes over.

```python
def make_multiplier(factor):
    def mul(x):
        return x * factor
    return mul

triple = make_multiplier(3)
print(triple(10))
```
- Что приходит на вход.
  - `factor=3` в `make_multiplier`.
  - Затем `x=10` в `mul`.
- Что делает код шаг за шагом.
  - `make_multiplier` создает внутреннюю функцию `mul`.
  - `mul` использует `factor` из внешней области видимости.
  - `make_multiplier` возвращает `mul`.
  - Затем `triple(10)` вызывает `mul(10)`.
- Что возвращается или печатается.
  - Печатается `30`.
- Подводный камень.
  - Ошибка начинающих. Думать, что `factor` исчезает после завершения `make_multiplier`. В замыкании он сохраняется.

В этом блоке ты учишься видеть границы видимости переменных и понимать, когда переменная берется из локальной области, а когда из внешней. Это помогает избегать ошибок с неожиданными значениями.

Если нужно изменить переменную не глобальную, а из внешней функции, используют `nonlocal`.

```python
def make_counter():
    count = 0

    def inc():
        nonlocal count
        count += 1
        return count

    return inc

c = make_counter()
print(c())
print(c())
```

## Объединяющий пример
Задача. Собрать список покупок, посчитать итог, и напечатать чек. Мы используем функции с параметрами по умолчанию, `*args`, `**kwargs`, и аккуратную передачу аргументов.

```python
def add_item(items, name, price, *tags, **meta):
    item = {
        "name": name,
        "price": price,
        "tags": list(tags),
        "meta": meta,
    }
    items.append(item)


def total(items, tax=0.0):
    subtotal = sum(item["price"] for item in items)
    return round(subtotal * (1 + tax), 2)


def print_receipt(items, title=None):
    if title is None:
        title = "Покупки"

    print(title)
    for item in items:
        print(f"- {item['name']}: {item['price']}")

    print(f"Итого: {total(items, tax=0.1)}")


items = []

add_item(items, "молоко", 90, "еда", producer="Вкусно", shelf_days=5)
add_item(items, "хлеб", 50, "еда")

extra_tags = ["акция", "срочно"]
extra_meta = {"comment": "взять сегодня"}
add_item(items, "яблоки", 120, *extra_tags, **extra_meta)

print_receipt(items)
```
- Что приходит на вход.
  - `items` как список словарей покупок.
  - `add_item` получает `name`, `price`, произвольные теги через `*tags` и метаданные через `**meta`.
  - `total` получает список покупок и параметр `tax` со значением по умолчанию.
  - `print_receipt` получает список покупок и `title`, который по умолчанию `None`.
- Что делает код шаг за шагом.
  - `add_item` собирает словарь товара и добавляет его в `items`.
  - `total` суммирует цены и применяет налог `tax=0.1`.
  - `print_receipt` печатает список покупок и итог.
  - В третьем добавлении используется распаковка `*extra_tags` и `**extra_meta`.
- Что возвращается или печатается.
  - Печатается заголовок `Покупки`.
  - Печатаются строки товаров.
  - Печатается `Итого: 286.0`.
- Подводный камень.
  - Если передать в `add_item` метаданные с ключами, которые совпадают с обязательными полями, можно получить дублирование данных. Например `name` окажется и в поле `name`, и внутри `meta`.
