# lesson 35 (15.12). Функции. параметры, значения по умолчанию, *args, **kwargs, область видимости

## Ключевые понятия

- Функция. именованный блок кода который повторяет одно действие столько раз, сколько нужно
- Заголовок функции. строка `def имя(параметры)` которая описывает входы
- Аргумент вызова. конкретное значение которое передается функции во время запуска
- Возврат. инструкция `return` которая отдает результат обратно вызывающему коду
- Докстринг. первая строка тела функции которая объясняет назначение и параметры
- Позиционный параметр. значение которого определяется порядком аргументов
- Именованный аргумент. запись `параметр=значение` которая убирает зависимость от порядка
- Значение по умолчанию. базовое значение которое используется если аргумент не передан
- Параметры `*args` и `**kwargs`. сборщики произвольных позиционных и именованных аргументов
- Область видимости. набор переменных доступных в конкретной точке программы

### 🟢 Функция как описанное действие

Функция выделяет повторяющийся шаг. Код становится похож на панель кнопок. Каждая кнопка запускает известное действие.

```python
def format_greeting(name):
    """Готовит персональное приветствие."""
    message = f"Привет, {name}!"
    return message

print(format_greeting("Ира"))
```

`return` завершает работу и отдает результат. Если `return` отсутствует, Python вернет `None`, поэтому явный возврат полезнее.

```python
def compare_numbers(a, b):
    """Сообщает, какое число больше."""
    if a > b:
        return "Первое больше"
    if a < b:
        return "Второе больше"
    return "Числа равны"

print(compare_numbers(8, 5))
```

Бытовая аналогия. функция похожа на кофемашину с понятными кнопками. Мы доверяем названию кнопки и получаем одинаковый результат.

### 🟡 Параметры, порядок и значения по умолчанию

Параметры описывают входные данные. Позиционные аргументы читаются по порядку, поэтому перепутанные значения ломают смысл сообщения.

```python
def create_profile(name, city, job):
    return f"{name} из {city} работает как {job}"

profile = create_profile(name="Илья", job="аналитик", city="Казань")
print(profile)
```

Значение по умолчанию сокращает повтор. Нельзя задавать изменяемые объекты по умолчанию, иначе данные будут накапливаться между вызовами.

```python
def add_tag(text, tags=None):
    if tags is None:
        tags = []
    tags.append("processed")
    return text, tags

print(add_tag("Привет"))
```

Жизненный пример. параметры похожи на форму заказа еды. Поля заполнены заранее популярными вариантами, а остальные значения вводятся вручную.

### 🟡 Работа с докстрингами и проверкой возврата

Докстринг объясняет, что делает функция. Первая строка описывает действие, остальные уточняют детали. Явный `return` помогает читать код, потому что видно, где функция отдает результат.

```python
def normalize_phone(raw_phone):
    """
    Принимает номер в свободном виде.
    Возвращает строку +7XXXXXXXXXX.
    """
    digits = [ch for ch in raw_phone if ch.isdigit()]
    last_ten = "".join(digits[-10:])
    return "+7" + last_ten

print(normalize_phone("8 (912) 555-44-33"))
```

Докстринг полезен при вызове функции из IDE. Подсказка появляется мгновенно и экономит время.

### 🔴 Гибкие аргументы *args и **kwargs

`*args` собирает дополнительные позиционные значения в кортеж. Это похоже на список пожеланий клиента. `**kwargs` собирает именованные опции в словарь, поэтому редкие настройки можно передавать прямо по ключу.

```python
def collect_scores(student, *scores, **meta):
    average = sum(scores) / len(scores) if scores else 0
    status = meta.get("status", "учится")
    return f"{student}. средний балл {average:.1f}. статус {status}"

print(collect_scores("Вика", 8, 9, 10, status="отличница"))
```

Гибкие аргументы можно прокинуть в другую функцию без распаковки. Такой прием строит цепочку вызовов без копирования кода.

```python
def log_event(event_name, *args, **kwargs):
    print("LOG:", event_name, args, kwargs)

def tracked_call(func, *args, **kwargs):
    log_event(func.__name__, *args, **kwargs)
    return func(*args, **kwargs)

def multiply(a, b):
    return a * b

print(tracked_call(multiply, 3, 7, note="quick calc"))
```

В реальности `*args` и `**kwargs` похожи на ящик с пожеланиями. Менеджер получает любые дополнения и передает их в отдел без переписывания форм.

### 🔴 Область видимости и замыкания

Локальные переменные живут только внутри функции. Попытка обратиться к ним снаружи вызывает `NameError`. Глобальные переменные можно читать, но запись требует слова `global`.

```python
def show_city():
    city = "Пермь"
    print(city)

show_city()
# print(city)  # NameError
```

Замыкание появляется, когда внутренняя функция использует переменные внешней функции. Такая внутренняя функция помнит окружение и ведет себя как персональный помощник.

```python
def make_discount(rate):
    def apply_discount(amount):
        return amount * (1 - rate)
    return apply_discount

student_discount = make_discount(0.15)
print(student_discount(1000))
```

Сценарий практики. Нужно построить сервис уведомлений о покупках. Понадобятся функции с параметрами, значения по умолчанию, сбор произвольных аргументов и замыкание, которое хранит курс бонусов.

## Объединяющий пример

Туристическая компания собирает отчет по поездке. Сценарий использует функции с параметрами, значения по умолчанию, гибкие аргументы, работу со словарями и замыкание для налогов.

```python
def make_tax_calculator(rate):
    def calc(amount):
        return amount * rate
    return calc

def build_item(name, price, quantity=1):
    return {"name": name, "price": price, "quantity": quantity}

def summarize_trip(customer, *items, discount=0, **options):
    subtotal = sum(item["price"] * item["quantity"] for item in items)
    subtotal -= discount
    tax_rate = options.get("tax_rate", 0.2)
    calc_tax = make_tax_calculator(tax_rate)
    tax = calc_tax(subtotal)
    currency = options.get("currency", "RUB")
    note = options.get("note", "стандартное бронирование")
    total = subtotal + tax
    return {
        "customer": customer,
        "items": items,
        "subtotal": subtotal,
        "tax": tax,
        "total": total,
        "currency": currency,
        "note": note,
    }

trip = summarize_trip(
    "Антон",
    build_item("перелет", 14500),
    build_item("отель", 8200, quantity=3),
    discount=500,
    tax_rate=0.12,
    currency="RUB",
    note="повторный клиент"
)

def format_report(record):
    """Формирует человекочитаемый отчет."""
    return (
        f"{record['customer']}: {record['total']:.2f} {record['currency']} "
        f"(налог {record['tax']:.2f}). {record['note']}"
    )

print(format_report(trip))
```

`make_tax_calculator` хранит ставку налога в замыкании. `build_item` применяет значение по умолчанию для количества. `summarize_trip` принимает `*items` и `**options`, использует именованные параметры и возвращает словарь. `format_report` читает словарь и печатает готовый текст. Сценарий объединяет все идеи урока.
