# lesson 33 (11.12). Генераторы коллекций. list, dict, set comprehensions

## Ключевые понятия

- Генератор коллекции. компактный способ создать новую коллекцию на основе существующей с помощью одного выражения
- List comprehension. выражение для создания списка из другой коллекции с опциональной фильтрацией элементов
- Dict comprehension. выражение для создания словаря из последовательности пар или из другой коллекции
- Set comprehension. выражение для создания множества, автоматически убирающего дубликаты элементов
- Источник данных. коллекция или итерируемый объект, по которому пробегает генератор коллекции
- Условие фильтрации. логическое выражение в comprehension, которое решает. включать элемент или нет
- Выражение преобразования. часть до for, которая описывает. какой именно элемент попадет в новую коллекцию
- Читаемость comprehension. ограничение сложности выражения, чтобы код оставался понятным
- Типичные ошибки comprehension. слишком сложные выражения, тяжелые вычисления внутри тела, путаница с вложенными циклами

### 🟢 Введение в генераторы коллекций

Генераторы коллекций помогают переписать простой цикл, который создает новую коллекцию, в одно компактное выражение. 

Классический код с циклом:

```python
numbers = [1, 2, 3, 4, 5]
squares = []
for x in numbers:
    squares.append(x * x)
```

Тот же эффект через list comprehension:

```python
numbers = [1, 2, 3, 4, 5]
squares = [x * x for x in numbers]
```

Важно понимать. генератор коллекции не меняет исходный список. он создает новый. 

На уровне джуна comprehension полезны там, где

- нужно просто преобразовать элементы
- нужна простая фильтрация по одному условию
- выражение легко читается вслух

Если внутри несколько уровней логики. вложенные условия, обработка ошибок, побочные эффекты. лучше оставить обычный цикл.

### 🟡 List comprehensions. базовый рабочий инструмент

List comprehension. самый распространенный вид генераторов коллекций. Он строит новый список на основе одного или нескольких источников данных.

Базовый шаблон.

```python
[new_value for item in source]
```

С фильтрацией по условию.

```python
[new_value for item in source if condition(item)]
```

Примеры.

1. Преобразовать строки в длины.

```python
names = ["Ann", "Bob", "Catherine"]
lengths = [len(name) for name in names]
# [3, 3, 9]
```

2. Оставить только четные числа.

```python
numbers = [1, 2, 3, 4, 5, 6]
even_squares = [x * x for x in numbers if x % 2 == 0]
# [4, 16, 36]
```

3. Нормализовать строки.

```python
raw_cities = ["  Moscow ", "spb", "Kazan  "]
clean_cities = [city.strip().title() for city in raw_cities]
# ["Moscow", "Spb", "Kazan"]
```

На что обратить внимание.

- Важно, чтобы внутри выражения не было сложной ветвистой логики
- Нежелательно вызывать тяжелые функции в части new_value. например сетевые запросы
- Хорошая проверка. если comprehension трудно прочитать вслух одним предложением. вероятно, стоит вернуться к обычному циклу

### 🟡 Dict и set comprehensions

Dict comprehension создает словарь из другой коллекции. Часто используется для переупаковки данных в формат ключ.значение.

Общий шаблон для dict.

```python
{key_expr: value_expr for item in source}
```

Пример. словарь квадратов чисел.

```python
numbers = [1, 2, 3, 4]
squares = {x: x * x for x in numbers}
# {1: 1, 2: 4, 3: 9, 4: 16}
```

Пример. словарь имени и длины.

```python
names = ["Ann", "Bob", "Catherine"]
name_to_length = {name: len(name) for name in names}
# {"Ann": 3, "Bob": 3, "Catherine": 9}
```

С фильтрацией по условию.

```python
products = [
    {"name": "Apple", "price": 100},
    {"name": "Banana", "price": 40},
    {"name": "Cherry", "price": 150},
]

expensive = {
    product["name"]: product["price"]
    for product in products
    if product["price"] > 100
}
# {"Cherry": 150}
```

Set comprehension создает множество. часто для выделения уникальных значений.

```python
cities = ["Moscow", "Kazan", "Moscow", "Perm"]
unique_cities = {city for city in cities}
# {"Moscow", "Kazan", "Perm"}
```

Здесь дубликаты автоматически отбрасываются. так как множество хранит каждый элемент только один раз.

Полезные сценарии для set comprehension.

- собрать уникальные значения поля. города, логины, теги
- быстро проверить наличие элемента через оператор in
- сравнить два множества. найти пересечение или разность

### 🔴 Читаемость, сложность и типичные ошибки

Генераторы коллекций выглядят кратко, но легко превратить их в запутанный код. Важно сознательно ограничивать сложность.

Рекомендации по читаемости.

- Один comprehension. одна простая идея
- Не более одного условия if внутри
- Не использовать вложенные циклы без веской причины
- Если появляется необходимость в обработке ошибок или сложных ветках. лучше вернуться к обычному циклу

Пример избыточной вложенности.

```python
# Плохо читается
result = [
    (user["id"], order["id"])
    for user in users
    for order in orders
    if user["id"] == order["user_id"] and order["total"] > 1000
]
```

Здесь лучше вынести логику во внешний цикл или отдельную функцию.

Типичные ошибки.

- Модификация внешнего состояния внутри comprehension. например list.append в теле цикла
- Слишком тяжелые операции внутри выражения. сетевые вызовы, медленный ввод вывод
- Сложные условия, которые трудно проверить глазами

Стоит помнить. comprehension. это инструмент для компактной записи простых преобразований коллекций. а не способ поместить всю бизнес логику в одну строку.
